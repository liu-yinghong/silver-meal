# 饭心·银龄放心单 v1.1.0 版本更新说明

> 版本：v1.1.0（自 v1.0.0 起）
> 更新日期：2026-08-02
> 主题：功能完善、体验优化、稳定性提升，为比赛演示与客户交付准备

---

## 一、新增功能

### 家属端
- **"已保存的饮食规则"独立页面**：从"我的"页进入，完整展示已保存的规则，支持返回与修改规则
- **"今日订单"页面**：底部导航进入，仅展示当天全部订单（餐品图、状态、价格、下单时间）
- **"订单记录"页面**：展示全部历史订单，按时间排序，含日期信息
- **首页"当前订单"卡片**：展示当前订单的餐品实拍图，便于家属直观识别

### 老人端
- **家属留言全链路**：家属发送携带真实称呼（如"女儿"）；老人端弹窗、留言聊天页、历史留言均显示发送者身份
- **餐品实拍图展示**：推荐页、配送页、收餐页均展示对应餐品的实拍图，缺图时自动回退占位图
- **AI 对话气泡**："AI正在为您安排"页的提示改为 AI 机器人旁的对话气泡，模拟机器人说话，提升展示效果
- **订单流程页面重设计**：
  - 配送页移除顶部独立信息框，餐品图并入状态窗口，全状态（下单中/制作中/配送中/已送达）均展示
  - 收餐确认页餐品图铺满订单信息窗口
- **本地推荐降级策略**：
  - 后端在线 + 大模型响应慢 → 可用本地推荐兜底，提升体验
  - 后端断开 → 大模型与本地推荐均不可用，明确提示"服务器已断开，请重试"，不显示任何假餐食

## 二、AI 能力

- **50 张餐品实拍图生成**：用通义万相按每个餐品名称+描述批量生成 1280×720 高清实拍图，替代原扁平插画
- **大模型调用优化**：
  - 远程调用默认不重试，最坏等待从 41s 降至 20s（与文档"不重试"一致）
  - 今日推荐天气挑选候选餐品 15→5，避免提示词过长导致超时降级，恢复"大模型按天气推荐"
  - 今日推荐增加 60 秒缓存，重复点击秒回（首次约 17s 为大模型耗时）

## 三、Bug 修复

- **预算硬过滤**：用户明确说出的价格上限从"仅加分"改为硬过滤——说"20元以内"绝不推荐超预算餐品
- **离线假成功 / 假定单**：移除所有 `ORD-OFFLINE`/`ORD-DEMO` 假定单创建；真实订单断网一律明确报错，不假装成功
- **今日订单空显示**：修复日期过滤误传订单对象导致列表为空的 bug
- **推荐页图片被隐藏**：删除 renderMeal 后误隐藏图片的代码，推荐套餐正常显示图片
- **底部导航高亮不切换**：高亮改为由当前页面统一决定，切换页面时橘色正确移动、返回时正确复位
- **`_start.bat` 无法启动**：修复 UTF-8 编码、换行符、无效字符污染、机器专属路径等问题，改为通用启动（自动寻找 uvicorn/python、端口占用时智能提示）

## 四、稳定性与测试

- **代码全面审核优化**：清理一次性开发脚本、未引用静态资源、缓存文件；补全依赖清单（openai/dashscope/python-multipart）
- **测试扩充至 67 个**：新增预算硬过滤、今日推荐、订单超时/取消、家属规则读写、天气降级、SSE 流式、联系家属等接口用例
- **全量验证**：所有接口连通、两端页面零 JS 报错、推荐→下单→配送→确认收餐关键流程端到端跑通

## 五、工程化

- **可复用开发技能**：沉淀"适老 Web 应用全栈开发"模式（适老 UI、SPA 路由、Repository 模式、LLM 降级、通义万相生图等），供后续类似项目复用
- **文档同步**：API 契约更新至 v0.1.1；README 补充预算硬过滤、离线行为、餐品图片等说明

## 六、说明

- 项目当前可稳定运行，后端 67 个自动化测试全部通过
- 适合比赛现场演示：核心卖点（大模型推荐、预算硬过滤、适老体验、离线兜底）均正常
- 已为淘宝生态接入预留扩展点（Repository 模式），后续新增 Taobao 数据仓库即可对接，API 层与前端无需改动
