# 饭心·银龄放心单 v1.2.0 版本更新说明

> 版本：v1.2.0（自 v1.1.2 起）
> 更新日期：2026-08-07
> 主题：统一版本号；强化客户环境可排查性与开箱即用体验；承接餐食图片修复与"未命中需求"兜底提示

---

## 一、统一版本号

- 项目版本统一为 **v1.2.0**（README 版本行、版本更新记录、后端 `app.main` 版本均一致）。

## 二、客户环境可排查性（本版重点）

针对"客户从 GitHub 拉取后大模型/语音不生效"的问题，新增一套开箱即用的自诊断能力：

- **`GET /api/status` 环境诊断接口**：返回大模型/语音的 `OPENAI_API_KEY` 是否已被后端读取（`llm.configured` / `asr.configured`），10 秒定位"没走大模型"问题。
- **ASR 无 Key 明确提示**：未配置 Key 时返回 `ASR_NO_KEY`，提示"请先设置环境变量并重启后端"，不再笼统报"识别失败"。
- **ASR 失败原因落后端日志**：`transcribe` 失败时把百炼实际返回的 `status/message/code` 与音频大小打印到后端窗口，可区分是 **Key 无效**、**网络不通**还是**录音过短/无语音**。
- **老人端推荐页"本地推荐模式"提示**：当 `ai_mode=local`（大模型未生效）时，推荐页顶部显示黄色提示"当前为本地推荐（大模型未生效），请检查 OPENAI_API_KEY 配置"，客户在页面上即可判断。
- **`_start.bat` 环境检查**：新增 `python -V` 有效性检测，识别 Windows"应用执行别名"/Microsoft Store 占位符并给出修复指引（关闭别名 + 安装真实 Python）。
- **README 常见问题**：新增"环境要求与常见问题"章节（Python 3.10+、Store 别名、装依赖失败、端口占用、必须在项目根目录启动、文字推荐不相干/语音 422 排查）。

## 三、承接此前更新

- **餐食图片在 `file://` 直接打开时无法显示**：图片地址统一按接口基址（`API.BASE_URL`）解析，http(s) 托管与双击打开均正常显示（老人端推荐/配送/收餐、家属端订单卡片/列表）。
- **"未命中需求"智能兜底提示语**：老人提到菜单里没有的餐食时，由大模型生成解释提示语并推荐符合家属规则的替代餐食；大模型不可用或超时自动降级为本地模板，不影响推荐主流程。

## 四、稳定性与测试

- **测试扩充至 72 个**：新增 `/api/status`、ASR 无 Key 提示等用例；两个 ASR 用例显式设置环境变量，保证在无 Key 的 CI 环境也能稳定通过。
- **全量验证**：从 GitHub 全新克隆 → 干净环境安装 `requirements.txt` → 配置 `OPENAI_API_KEY` → 用 `_start.bat` 启动：
  - `/api/status` 显示 `configured=true`、`mode=remote`；
  - 文字推荐由大模型按需求生成（如"想吃清淡的，30元以内"→ 三份低油低盐且 ≤30元；"想吃红烧排骨饭"→ 红烧排骨饭置顶命中）；
  - 真实语音识别返回 200 并转出文字；
  - 页面与餐品图片正常显示，零报错。

## 五、说明

- 若客户在大模型/语音上仍有异常，按 README"环境要求与常见问题"排查：设置 `OPENAI_API_KEY` 后**必须重启后端**；再访问 `/api/status` 确认 `configured=true`；若仍失败，后端日志中的 `[ASR]` 行可精确定位原因。
