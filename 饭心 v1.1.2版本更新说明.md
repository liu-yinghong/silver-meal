# 饭心·银龄放心单 v1.1.2 版本更新说明

> 版本：v1.1.2（自 v1.1.1 起）
> 更新日期：2026-08-06
> 主题：修复餐食图片显示问题，新增"未命中需求"智能兜底提示语，体验与稳定性完善

---

## 一、新增功能

### 老人端
- **"未命中需求"智能兜底提示语**：当老人提到的餐食在菜单中不存在时（例如说"我想吃龙肉"，菜单里没有龙肉），推荐结果页顶部会由大模型生成一句口语化、亲切的解释提示语，说明"没有找到"的原因，并同时给出合适的替代推荐（例如自动挑选龙利鱼、海参、鲈鱼等"与肉/鱼有关"的套餐）。提示语与替代餐食均写入推荐结果，老人无需等待即可看到推荐。
  - 提示语由大模型生成，说清楚"为什么没有"+"已推荐了什么"；
  - 替代推荐餐食**始终校验符合家属规则**（预算、禁忌食材、饮食偏好），推荐安全合规；
  - 大模型不可用、未配置 Key 或超时时，**自动降级**为本地模板提示语（"抱歉，暂时没有找到与「…」完全匹配的餐食，为您挑选了下面几份比较合适的"），不影响推荐主流程。

## 二、Bug 修复

- **餐食图片在 file:// 访问下不显示**：修复餐食图片根相对路径（`/elder/images/...`）在直接双击打开 HTML（`file://` 协议）时被浏览器错误解析到本地盘根目录、导致图片加载失败的问题。图片地址统一按接口基址（`API.BASE_URL`）解析，**http(s) 托管与双击打开两种方式均能正常显示**。
  - 覆盖老人端：推荐结果页、配送状态页、收餐确认页；
  - 覆盖家属端：今日状态卡、今日订单列表、历史订单记录。
- **家属端订单餐品图片显示**：家属端订单卡片/列表的餐品图片统一修复，缺图时回退占位图，不出现裂图。

## 三、稳定性与测试

- **测试扩充至 70 个**：新增"未命中需求兜底"用例（未命中时返回说明提示语、替代餐食仍符合家属规则、命中时不触发提示语）。
- **全量验证**：推荐 → 下单 → 配送 → 确认收餐关键流程端到端跑通；两端页面零 JS 报错；推荐接口在未命中场景返回替代推荐而非报错。

## 四、说明

- 版本号更新为 **v1.1.2**，README 同步完善（版本行、功能说明、版本更新记录）。
- 大模型兜底提示语依赖阿里云百炼接口；未配置 `OPENAI_API_KEY` 或接口超时时自动降级为本地模板，不影响推荐主链路。
- 承接 v1.1.1 已发布内容：家属端订单餐品图片、服务器连接失败提示、语音麦克风权限优化（仅询问一次、会话内复用）、通用一键启动脚本。
