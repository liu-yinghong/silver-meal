# 饭心·银龄放心单 v1.2.1 版本更新说明

> 版本：v1.2.1（自 v1.2.0 起）
> 更新日期：2026-08-14
> 主题：全面适配手机端访问，保证电脑端与手机端页面、功能一致；优化启动脚本与缓存刷新

---

## 一、手机端适配（本版重点）

针对"电脑端正常、手机端打开后页面显示异常"的问题，对**老人端**与**家属端**前端做全面响应式适配：

- **手机全宽布局**：两端页面在手机浏览器下自动铺满全屏宽度（`width:100%` + `max-width:430px` + `viewport-fit=cover`），内容、字号、按钮、卡片均与电脑端一致，无横向滚动。
- **高度兼容**：`100dvh` 前补齐 `100vh` 兜底、`aspect-ratio` 容器补 `height` 兜底，兼容老版本手机浏览器。
- **底部导航稳健锚定**：两端底部导航由 `position:fixed` 改为 flex 锚定（`margin-top:auto`），**不再依赖 `position:fixed`**——即使在腾讯元宝等 App 的内嵌 WebView（缩放/变换容器）里也能贴底显示，不会跑到页面中部。
- **桌面端修复**：桌面端 `.page--active` 高度与容器对齐，避免底部导航被 `overflow:hidden` 裁切；`pageIn` 动画移除 `transform`，防止 WebKit 残留 transform 破坏后代元素定位。
- **缓存刷新**：两端 CSS 版本号更新为 `?v=20260813a`，手机端首次访问自动拉取新样式。

## 二、启动脚本优化（`_start.bat`）

- **自动检测并显示局域网 IP**：启动横幅额外显示 `Phone access: http://<局域网IP>:8000/elder/`，手机与电脑连同一 Wi-Fi 即可直接访问。
- **端口占用可选重启**：端口 8000 被占用时，输入 `R` 自动停止旧进程并重新启动，或直接回车打开已运行页面（不再只能被动等待）。
- 保持 ASCII + CRLF 编码，双击不会闪退。

## 三、稳定性与测试

- **后端测试 72 个全部通过**（推荐过滤 / 未命中兜底 / 订单状态机 / API 冒烟 / LLM 降级）。
- **双端双端到端验证**：
  - 桌面端（1280×900）：老人端/家属端单页面显示、430px 卡片居中、底部导航完整可见、零 JS 报错；
  - 手机端（390px、iPhone UA）：两端全宽、无横向溢出、单页面、导航贴底、零 JS 报错；
  - 关键流程：推荐 → 下单 → 配送 → 确认收餐链路正常，餐品图片正常显示。

## 四、说明

- 承接 v1.2.0 已发布内容（`/api/status` 环境诊断、ASR 优化、`OPENAI_API_KEY` 配置说明、餐食图片修复、未命中兜底提示）。
- 手机访问需与电脑同一局域网，且使用手机 **Safari/Chrome/微信浏览器**（内嵌 WebView 容器可能影响页面布局，属宿主环境限制）。
- 电脑端访问方式不变：`http://localhost:8000/elder/`、`http://localhost:8000/family/`。
